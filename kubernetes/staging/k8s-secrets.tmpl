apiVersion: v1
kind: Secret
metadata:
  name: medical-history-secrets
stringData:
  DB_USER: "{{DB_USER}}"
  DB_PASSWORD: "{{DB_PASSWORD}}"
  DB_NAME: "{{DB_NAME}}"
  DB_PORT: "{{DB_PORT}}"
  DB_HOST: "{{DB_HOST}}"